pipeline{
    agent{
        label 'Lahari'
    }
    triggers{
        pollSCM('* * * * *')
    }
    stages{
        stage('terraform-execution'){
            steps{
                script{
                    sh """
                        cd terraform-jenkins/
                        terraform init
                        terraform fmt
                        terraform validate
                        terraform plan
                        terraform apply -auto-approve
                    """
                }
            }
        }
    }
}
